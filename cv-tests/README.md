## How to install:

```
git submodule update --init --recursive

# "Download the model as per the EAST README.md"

pip install -r requirements.txt
# Make sure you also got the opencv3.1 binaries installed somewhere
# Try conda install -c menpo opencv
```